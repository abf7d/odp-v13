import {FilterByDrugclassPipe} from './filter-by-drugclass.pipe';

describe('FilterByDrugclassPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByDrugclassPipe();
    expect(pipe).toBeTruthy();
  });
});
